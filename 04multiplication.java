class multiplication
{
    public static void main(String args[])
    {
      int a = 5 , b = 8 , multi; //defined values in compile time
      multi = a * b; //formula
        System.out.println("multiplication of two numbers is =" + multi); //plus sign used to concate the strings 
    }
}
